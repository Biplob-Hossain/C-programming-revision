#include<stdio.h>

int main()
{

    int n, fact=1;

    printf("Enter n number to check Factorial");
    scanf("%d",&n);

    for (int i = 1; i <= n; i++)
    {
        fact=fact*i;

       
    }
     
    printf("Factorial number %d\n",fact);
    
}
