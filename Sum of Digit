#include<stdio.h>

int main()
{
     int N, temp, rem, sum=0;

     printf("ENter any Digit for sum\n");

    scanf("%d",&N);

    temp=N;
    while (temp!=0)
    {
        rem=temp%10;
        sum=sum+rem;
        temp=temp/10;
    }
    printf("Sum of Digit is %d ",sum);
    return 0;
    

}
