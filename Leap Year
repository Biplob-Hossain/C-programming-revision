#include<stdio.h>

int main()
{

int year;

printf("Please enter any year to check Leap year or not\n");

scanf("%d",&year);

if ((year%4==0 && year%100 !=0) || (year %400==0))
{
    printf("This is a Leap year",year);
}
 else
 printf("This is not a LeapYear",year);

}
    
